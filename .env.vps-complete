# Variables d'environnement pour VPS production isolé (Port 4000)
# ⚠️ IMPORTANT: Changez TOUS les mots de passe et secrets !

# Base de données MySQL (port isolé pour VPS partagé)
DB_HOST=db
DB_USER=immigreat_user
DB_PASSWORD=Immigreat2024SecurePassword!
DB_NAME=immigration_production
DB_ROOT_PASSWORD=RootPassword2024Secure!
DB_PORT=3306

# Application Node.js (port isolé)
APP_PORT=4000
PORT=4000
NODE_ENV=production
JWT_SECRET=ImmigReatJWTSecretKey2024WithMoreThan32Characters!
FRONTEND_URL=http://195.35.14.15:4000

# Configuration serveur
TZ=America/Montreal

# Ports isolés pour VPS partagé
MYSQL_EXTERNAL_PORT=3307
NGINX_HTTP_PORT=8080
NGINX_HTTPS_PORT=8443

# Sécurité
SESSION_SECRET=ImmigReatSessionSecret2024VerySecure!
ENCRYPTION_KEY=MyEncryptionKey2024For32Characters!

# Optionnel: Configuration email (si votre app envoie des emails)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=votre-email@gmail.com
# SMTP_PASS=votre-mot-de-passe-app

# Optionnel: Monitoring et logs
# SENTRY_DSN=https://votre-sentry-dsn
LOG_LEVEL=info

# Configuration uploads
MAX_FILE_SIZE=10mb
UPLOAD_PATH=/usr/src/app/uploads

# Configuration base de données avancée
DB_CONNECTION_LIMIT=10
DB_TIMEOUT=60000

# Variables pour le déploiement Docker
COMPOSE_PROJECT_NAME=immigreat-4000
DOCKER_BUILDKIT=1
